# 青石江湖实施与验收总记录

更新：2026-09-07。仅记录当前版本结论。P0、P1 本地技术实现与技术验收完成；用户已明确选择稍后真人试玩，真人验收未记为通过。P2 未开始。本轮没有提交、推送或发布。

## 当前实现

- 保留六十日十九事件、五条人生方向、序章三种可接续暂结、十条端到端通关样本，以及关系记忆、经济、三账、成长、战斗、地图、存档等原系统。
- 九张立绘使用构建基路径，换角色重新建立资源加载状态。根站和项目子路径共用资源登记；未知角色或图片失败有明确文字占位。
- 八幅原创代码绘制水墨 SVG 覆盖荒道、城门、客栈、医馆、码头、盐仓、县衙、破庙。开场、地点、终局同步更新图与地点说明；窄屏保留环境文字。资源失败不影响对话和选项。
- 本地桌面 `/` 正确映射实际 HTML 入口；Pages 项目内地图采用片段导航，返回不跳离项目目录。未修改已发布网站。
- 战斗失物文案使用中文材料名。旧档表达中的英文材料、来源等仅由显示投影转换，原始历史和内部兼容标识保留。这里验收的是玩家可见内容；不宣称 v6 内部键已从程序包中删除。
- 唯一详细分支图登记十九事件和九类特殊场景，包括城门、客栈、医馆、证物、关系、序章暂结及三类人生终局。审查准入检查模板、反馈文案和实际信息来源，新增分支未登记将触发测试失败。
- 三类核心证明各有三个独立场景来源，并实际执行取证与存读。序章尸体实名属于可选封存分支的局部核验，未将其误报为全篇三路径强制门槛。
- 背包只显示实际持有物和证据，包含概览、已知来源、用途及当前不可用原因。使用需再次确认，复用当前有限选项和裁决，不恢复被扣押、交存或遗失的实物。
- 下一步引导依据现状提示问路、治疗、来信、谋生与缺席消息，不代选路线。原沉默与拒绝由即时台词、话题关闭和次日 NPC 行为表现，不新增亲密度数值或永久沉默计数。
- 修复打听消息期间死亡后仍把未听到的事件标为已知的问题；死亡即停止消息接收。

## 存档策略

GameState 仍为 v6，存储键仍为 `qingshi-jianghu-save-v5`。本轮没有新增持久字段、账本或迁移脚本；UI 加载、背包展开、使用确认等均是组件内存。合法 v5/v6 继续经原解码器补已有安全默认值，不补写未经历的沉默、承诺和立场。原有伪造、矛盾、部分字段缺失及备份回退测试全部保留并通过。

## 修改文件

| 文件 | 用途 |
| --- | --- |
| `app/page.tsx`、`app/map/page.tsx`、`app/globals.css` | 资源与场景接入、背包及确认、下一步、窄屏和降级 |
| `app/reliable-image.tsx`、`app/inventory-panel.tsx` | 可恢复的图像加载和背包呈现 |
| `lib/game/portraits.ts`、`lib/game/campaign.ts` | 立绘基路径、损失材料中文名、打听死亡冻结 |
| `lib/ui/asset-path.ts`、`desktop-route.ts`、`scenes.ts`、`player-text.ts`、`inventory.ts` | 路径、场景、旧文本和背包只读投影 |
| `desktop/main.tsx`、`vite.desktop.config.ts` | 子路径导航和本地首页入口 |
| `public/scenes/*.svg` | 八幅原创场景，无外部素材 |
| `tests/day12-flow.test.ts` | D22 改为核验现行唯一分支图，未恢复旧文件 |
| `tests/p0-presentation.test.ts`、`image-fallback.test.ts` | V01–V05，路径、资源、显示转换、失败恢复和导航 |
| `tests/scene-audit.test.ts`、`npc-refusal-echo.test.ts`、`rumor-freeze.test.ts` | A01–A07，审查、缺席、替代取证、拒绝回声、物品与冻结 |
| `tools/scene-review.ts`、`render-scene-review.mjs` | 内部准入清单及唯一分支图登记生成 |
| `tools/check-resource-builds.mjs` | 本机 HTTP 与构建资源核验，无发布能力 |
| `README.md`、`后续需求文档.txt`、现行设计/验收文档 | 当前规则、审查矩阵、P2边界与验证结果同步 |

## 自动化结果

最后完整执行 `npm run check` 与 `npx tsc --noEmit`，通过。`check` 包含下列四项：

| 命令 | 结果 |
| --- | --- |
| `npm test` | **186 项通过、0 失败**；原 174 项保留，新增 12 项，D22更新失效引用 |
| `npm run lint` | 通过 |
| `npm run build` | Web 构建通过 |
| `npm run desktop:build` | 桌面前端构建通过 |
| `npx tsc --noEmit` | 类型检查通过 |

新增覆盖：十九事件逐项缺席和打听、无参与不授证、重复行动无效、三账各三个实际来源、城门沉默不生成自报、医馆拒绝不生成诊断、客栈拒绝登记的次日回声与知识隔离、完整旧字段缺失安全默认和部分缺失拒绝、扣押物不恢复、死亡/拘押/终局限制、打听中死亡不追加已知消息。既有十条完整人生路线、NPC关系、三账、技能、战斗、v5/v6、伪造存档与自动备份回退仍全部通过。

## 资源验证

- 本地 `npm run desktop:web`：`http://127.0.0.1:1420/` 返回 **200**，差役立绘返回 **200** 且内容为 SVG。
- 根路径桌面构建和额外 `--base=/qingshi-jianghu/ --outDir dist-pages-preview` 构建：各自首页、入口脚本与 17 份 SVG（九立绘、八场景）经本机 HTTP 校验通过。
- Web 的 `dist/client` 中 17 份资源均存在，来源与其他构建一致。
- 当前公开资源 `https://liuran-l.github.io/qingshi-jianghu/portraits/ma-sandao.svg` 经系统 curl 返回 **200**，正文确认是 SVG。PowerShell 客户端曾遇到 TLS EOF，改用系统 curl 成功，未关闭证书校验。
- 公开站点没有发布本轮改动，因此只确认现有公开立绘资源可达；新场景与新导航的 Pages 验收是本地同子路径构建，不冒称公开版本已更新。

## 验收边界与后续

用户已选择“稍后试玩，先完成技术验收”。没有真人首次游玩结论；没有真实浏览器鼠标点击、屏幕阅读器、浏览器像素级截图或原生 Tauri 包/SQLite 实机验收。图像失败和切换由组件实际回调及静态渲染测试验证，导航与资源另有路径和 HTTP 验证，不把它们称为真机视觉验收。

稍后真人应核对：首次能否理解下一步、看到角色与场景、理解物品用途、感知沉默后果，以及遗漏可选互动仍能继续。反馈前不扩展 P2。仍有约 545 kB 桌面主包大小提示、vinext 路由静态分类提示和本机 npm `python` 配置提示，均不是构建失败。

复现资源验证：先运行完整检查，再执行 `npm run desktop:build -- --base=/qingshi-jianghu/ --outDir dist-pages-preview` 和 `node tools/check-resource-builds.mjs`。本轮到此停止，不提交、不推送、不发布。
